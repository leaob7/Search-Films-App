import { StyleSheet } from "react-native";

export const stylesFav = StyleSheet.create({
  upFavBar: {
      flexWrap: "wrap",
      flexDirection: "row",
      justifyContent:"center",
      alignItems: "center",
      marginTop: 100,
  },
  upFavText: {
    color: "#d1b100",
    fontWeight: "bold",
    fontSize: 20,
  },
  favList: {
    marginTop: '30%',
  }
})
