import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  loadingContainer: {
    margin: "auto",
    alignItems: "center",
    justifyContent: "center",
  },
  loadingText: {
    color: "#d1b100",
    fontWeight: "bold",
    fontSize: 20,
    marginLeft: 135,
  }
})
